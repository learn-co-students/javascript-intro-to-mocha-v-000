const theTruth = () => {
  return true;
};

const sayHey = () => {
  return 'Hey!';
};

const sayHeyFriend = (name) => {
  return `Hey, ${name}!`;
};

const favoriteIceCream = (name) => {
  return `I love ${name}`;
};

const shouting = (str) => {
  return str.toUpperCase();
};

const roundDown = (n) => {
  return Math.floor(n);
}