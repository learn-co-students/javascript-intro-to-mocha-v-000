'use strict';

function favoriteIceCream(name) {
    return `I love ${name}`
}

function shouting(string) {
  return string.toUpperCase();
}

function roundDown(number) {
  var new_number = Math.floor(number);
  return new_number;
}

function theTruth() {
  return true;
}
